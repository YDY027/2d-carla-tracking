# 核心依赖（必须装的）
carla>=0.9.14,<0.9.15  # 只适配你的 CARLA 0.9.14，避免装错版本
ultralytics>=8.0.0     # YOLO 模型依赖，兼容高低版本（解决之前的 YOLO.list() 错误）
opencv-python>=4.5.0   # 图像处理（绘制边界框、显示窗口）
numpy>=1.21.0          # 数值计算（卡尔曼滤波、SORT 跟踪用到）

# 可选依赖（Python 内置，写了更清晰，不写也能运行）
argparse>=1.4.0        # 命令行参数（比如 --num_npcs、--conf-thres）
logging>=0.5.1.2       # 日志输出（代码里的打印信息）